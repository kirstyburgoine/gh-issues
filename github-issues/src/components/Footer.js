import React from 'react';

// Stateless Functional Component
// Only going to output static HTML so no eed for more
const Footer = () => {
	return <footer role="contentinfo" />;
};

export default Footer;
